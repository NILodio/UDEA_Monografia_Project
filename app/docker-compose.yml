version: '3'
services:
  fastapi:
    build: BackEnd/
    image: ddiazva312/fastapi:latest
    ports: 
      - 8080:80
    volumes:
      - ../models:/app/models
    container_name: fastapi
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #       - capabilities: [gpu]

  streamlit:
    build: FrontEnd/
    image: ddiazva312/streamlit:latest
    depends_on:
      - fastapi
    ports: 
        - 5000:5000
    container_name: streamlit
